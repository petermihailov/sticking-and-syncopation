export const TEMPO = {
  MIN: 20,
  MAX: 300,
  DEFAULT: 130,
} as const

export const FLAM = {
  OFFSET_MIN: 0.02,
  OFFSET_MAX: 0.05,
  OFFSET_TEMPO_MULTIPLIER: 0.08,
  GAIN_MULTIPLIER: 1.5,
} as const
